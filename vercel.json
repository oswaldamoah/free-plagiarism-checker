{
  "version": 2,
  "builds": [
    { "src": "api/edge.ts", "use": "@vercel/edge" },
    { "src": "package.json", "use": "@vercel/static-build", "config": { "distDir": "dist" } }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/api/edge.ts" }
  ]
}
